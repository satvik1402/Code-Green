# Copy this file to .env and update the values

# Flask secret key (generate a strong random key for production)
FLASK_SECRET_KEY=your-secret-key-here

# Database configuration
DATABASE_URL=sqlite:///plagiarism_detector.db

# CodeBERT model path (if using local model)
# CODEBERT_PATH=models/codebert-base

# Optional: Debug mode (set to False in production)
FLASK_DEBUG=True

# Optional: Logging level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO
